# Server & Frontend URL Configuration
PORT=5000
JWT_SECRET=request-mgt-system-super-jwt-token-secret-fallback-2026
APP_URL=http://localhost:5000
FRONTEND_URL=http://localhost:3000

# SMTP Connection Credentials (Mailtrap / SendGrid / AWS SES / Custom)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
SMTP_PASS=your_smtp_password
SMTP_SECURE=false
SMTP_FROM="Service Request Management System <no-reply@requestsystem.com>"
SMTP_FROM_NAME="Service Request Management System"
SMTP_FROM_EMAIL="no-reply@requestsystem.com"

# MongoDB Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/request_management_db?retryWrites=true&w=majority
